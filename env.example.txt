# LLM Comparison Tool - Environment Variables Example
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control!

# Server Configuration
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API Keys (Optional - can be set server-side instead of client-side)
# These are optional if you want to use server-side API keys
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...
# COHERE_API_KEY=...
# GROK_API_KEY=...
# OLLAMA_API_KEY=... (Optional - only needed for remote Ollama instances with authentication)

# Ollama Configuration
# OLLAMA_BASE_URL=http://localhost:11434 (Default: http://localhost:11434)

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_MAX_PROMPT_LENGTH=10000

# Security Configuration
ALLOWED_ORIGINS=http://localhost:3000
ENABLE_CORS=true

# Request Configuration
MAX_REQUEST_SIZE_MB=10
REQUEST_TIMEOUT_MS=300000  # 5 minutes (300000ms) - increase if you need longer waits

# Security Configuration
CSRF_SECRET=change-me-in-production-generate-random-secret
API_TOKEN_SECRET=change-me-in-production-generate-random-secret
AUDIT_LOGGING_ENABLED=true
AUDIT_LOG_MAX_ENTRIES=10000
ENABLE_DYNAMIC_IP_BLOCKING=true
MAX_VIOLATIONS_BEFORE_BLOCK=5
IP_BLOCK_DURATION_MS=3600000

# IP Filtering (comma-separated)
IP_WHITELIST=
IP_BLACKLIST=

